import { Leaderboard, updateLeaderboard } from "./leaderboard";
import {
  emptyUserDocument,
  updateUserDocument,
  UserDocument,
} from "./userDocument";

export type { Leaderboard, UserDocument };
export { emptyUserDocument, updateLeaderboard, updateUserDocument };
